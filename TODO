
lagere prioriteit

- create installer_root by copying system_root ?

- move patches upstream, eg.

  * libpng

  * python x-compile.

  * cygwin GCC

- GUILE bouwen zonder locale devel packages.

- update packages:

- Python: should compile .pyc with matching local python version.
=> add to local packages.

- Group spec/patch/source files by package

- gcc 4.1 for linux
- strip lily specific stuff from installer.py 

- Bugfixes:

  * fontconfig 2.4; Blocker: mingw mmap() compat. support.

- junk Makefile, create python based driver.
